
#set(GNULIB_LIBRARY gnulib)
#set(LISTLIB_LIBRARY listlib)
#set(GNULIB_LIB gnulib)
#set(GNULIB_LIB gnulib)

add_subdirectory(${LISTLIB_DIR})
add_subdirectory(${GNULIB_DIR})
add_subdirectory(${HDBSCAN_JAVA_DIR})
add_subdirectory(${HDBSCAN_PYTHON_DIR})
